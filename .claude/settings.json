{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "python3 -c 'import json,sys; d=json.load(sys.stdin); fp=(d.get(\"tool_input\") or {}).get(\"file_path\",\"\") or \"\"; norm=fp.replace(chr(92),\"/\").lower(); print(json.dumps({\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blocked: prototypes/ is protected. CLAUDE.md says never edit anything under prototypes/, not one character. Copy content out into review/ or elsewhere instead of editing in place.\"}})) if \"/prototypes/\" in norm else None'"
          }
        ]
      }
    ]
  }
}
